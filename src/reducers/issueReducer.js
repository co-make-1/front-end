const issueState = {
    id: "",
    title: "",
    description: ""
}

const issueReducer = (state = issueState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default issueReducer;