const userState = {
    id: "",
    name: "",
    email: "",
    bio: ""
    // votes: ""
}

const userReducer = (state = userState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default userReducer;